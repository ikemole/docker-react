language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t ikemole/docker-react -f Dockerfile.dev .

scripts:
  - docker run -e CI=true ikemole/docker-react npm run test
